# This is an example file. In a real deployment, you should copy this file to .env
# and replace the placeholder value with a strong, randomly generated secret.
# You can generate a strong secret key using a command like: openssl rand -hex 32

SECRET_KEY=YOUR_SUPER_SECRET_KEY_REPLACE_ME
